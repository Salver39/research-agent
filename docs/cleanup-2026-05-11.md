# Cleanup session — 2026-05-11

Подготовка проекта Research Preparation Agent к публикации на GitLab для оценки. Все изменения сделаны в рамках одной сессии.

---

## 1. Удаление лишних файлов и папок

**Размер проекта: \~742 МБ → 892 КБ** (в 830 раз меньше).

### Удалено

| Путь | Размер | Тип |
| --- | --- | --- |
| `frontend/node_modules/` | 330 МБ | артефакт сборки (переустановлен) |
| `backend/.venv/` | 359 МБ | виртуальное окружение (пересоздано) |
| `frontend/.next/` | 52 МБ | артефакт сборки |
| `backend/outputs/` | 896 КБ | сгенерированные тестовые .docx |
| `backend/research_agent.db` | 192 КБ | SQLite с тестовыми сессиями |
| `backend/chroma_db/` | 164 КБ | векторные индексы тестовых сессий |
| `frontend/tsconfig.tsbuildinfo` | 83 КБ | кэш TypeScript |
| `backend/documents/templates/` | — | пустая папка |
| `backend/uploads/` | — | пустая папка |
| `backend/prompts/` | — | пустая (позже пересоздана для п. 6) |
| `.DS_Store` (рекурсивно) | — | macOS-мусор |

### Сохранено

- **`examples/`** (новая папка в корне) — 6 готовых `.docx` из реальной сессии для портфолио (225 КБ)
- `research_agent_web_spec.docx` (корень) — спека проекта
- `.claude/` и `nimbalyst-local/` — остались **локально**, в репозиторий не попадут (см. п. 2)

---

## 2. Обновлён `.gitignore`

Добавлено:
- `*.tsbuildinfo`
- `.claude/`
- `nimbalyst-local/`

---

## 3. Переустановка зависимостей

После очистки:
- `cd frontend && npm install` — 393 npm-пакета
- `cd backend && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt` — 16 pip-зависимостей

---

## 4. Security-обновления frontend

| Пакет | Было | Стало |
| --- | --- | --- |
| `next` | 14.2.29 | **14.2.35** |
| `eslint-config-next` | 14.2.29 | 14.2.35 |

Patch-апдейт закрыл **6 из 11** advisories по Next.js (DoS, request smuggling, image optimization).

Оставшиеся 5 advisories требуют миграции на Next 16 + React 19 (major breaking change — отложено). В `README.md` добавлен раздел **Security notes** с описанием каждого оставшегося advisory и его применимости к проекту.

---

## 5. Дедупликация кода (низкорисковые правки)

### 5.1 `_clean_json` → один источник

- В `backend/api/stream.py` удалена локальная `_clean_json()`
- Используется импорт `_clean_json_text` из `backend/agents/base.py`

### 5.2 RAG: общий `get_collection()`

- **Создан** `backend/rag/client.py` с `get_collection()` и `CHROMA_PERSIST_DIR`
- В `backend/rag/indexer.py` и `backend/rag/retriever.py` дубликаты удалены, импорт из `rag.client`
- Lazy-импорт `chromadb` (внутри функции) сохранён

### 5.3 `<StreamingDots/>` UI-компонент

- **Создан** `frontend/components/StreamingDots.tsx`
- Применён в:
  - `frontend/components/wizard/MethodScreen.tsx`
  - `frontend/components/wizard/SamplingScreen.tsx`
  - `frontend/app/session/[id]/page.tsx` (компонент `ContextAnalyzing`)
- Другие streaming-индикаторы (`DesignScreen`, `HypothesesScreen`, `ResearchDiagnosisScreen`, `BriefScreen`) **намеренно не тронуты** — у них другие паттерны (с таймером / hung-warning / partial JSON parsing).

### 5.4 Комментарий в `orchestrator.py`

Рядом с `_get_design_agent()` добавлен комментарий о намеренной разделённости трёх Design-агентов (объясняет ревьюеру, почему они не унифицированы).

### Отложено (высокий регрессионный риск)

- **Унификация трёх Design-агентов** в `BaseParallelDesignAgent` — критичный user-path, нет автотестов. Намеренные различия (форматы вывода `tasks` / `guide_blocks` / `guide_blocks+meta+intro`, специфика merge, лимиты токенов).
- **Общий \****`_build_research_context()`** для агентов — переформулировка контекста в промптах может незаметно сдвинуть качество LLM-ответов.

---

## 6. Вынос длинных SYSTEM-промптов в отдельный модуль

### Создано

```
backend/prompts/
├── __init__.py
├── design.py              # 2124 chars
├── design_interviews.py   # 7304 chars
└── design_usability.py    # 6194 chars
```

### Изменено

В агентах inline `SYSTEM = """..."""` заменён на `from prompts.X import SYSTEM`:

| Файл | Было (chars) | Стало (chars) | Уменьшение |
| --- | --- | --- | --- |
| `backend/agents/design.py` | 7007 | 4901 | **-30%** |
| `backend/agents/design_interviews.py` | 15175 | 7900 | **-48%** |
| `backend/agents/design_usability.py` | 13612 | 7446 | **-45%** |

### Гарантия качества

Перенос выполнен Python-скриптом, который автоматически:
1. Сохранил оригинальные `SYSTEM` в `/tmp/prompt_backups/`
2. Создал новые prompt-файлы
3. Переписал agent-файлы с импортом
4. Сравнил новые `SYSTEM` с бэкапом **байт в байт**

В OpenAI уходит ровно тот же текст — **качество ответов агентов не изменилось**.

---

## 7. Чистка логирования

15 `logger.info` понижены до `logger.debug` в 4 файлах:

| Файл | Downgrade |
| --- | --- |
| `backend/orchestrator.py` | 2 |
| `backend/api/stream.py` | 3 |
| `backend/agents/design_interviews.py` | 4 |
| `backend/agents/design_usability.py` | 6 |

### Сохранены без изменений (важные операционные логи)

| Файл:строка | Лог |
| --- | --- |
| `backend/api/stream.py:76` | `logger.exception("Stream error for session %s", ...)` |
| `backend/agents/design_usability.py:78` | `logger.error("Block %d failed: %s", ...)` |
| `backend/agents/design_usability.py:91` | `logger.error("Hypothesis block %d JSON error...")` |

### Эффект

- **В prod (default INFO):** stdout/file-логи чистые от шума («Frame parsed OK», «State persisted», «asyncio.gather done in 8.5s»).
- **При отладке:** уровень переключается на DEBUG — видно полный поток как раньше.
- **Ошибки и стек-трейсы:** работают как прежде.

---

## Что НЕ сделано в этой сессии

Пункты, оставшиеся в плане аудита:

1. **🚨 Секреты в \****`backend/.env`** — файл содержит реальные API-ключи Anthropic и OpenAI. Нужно:
  - Отозвать существующие ключи в дашбордах OpenAI Console и Anthropic Console
  - Сгенерировать новые
  - Удалить из `.env` или почистить файл (он уже в `.gitignore`, но если коммитился ранее — ключи в истории)

2. **`backend/agents/design_survey.py`** — stub с TODO. Ветка в orchestrator выполняется при `method_key="survey"`, но системный промпт пустой → агент тихо возвращает мусор → состояние не сохраняется → пользователь застревает на «Не удалось сгенерировать». Варианты решения:
  - Реализовать (feature-work, ~1-2 дня)
  - Удалить файл + ветку в orchestrator (fallback на `DesignAgent`, но формат не соответствует методу)
  - Скрыть survey из UI и mapping в `types/research.ts`, `method.py`

3. **Разбиение больших React-компонентов** (`DesignScreen.tsx`, `HypothesesScreen.tsx`, `session/[id]/page.tsx`) — отказано. Файлы большие, но критичны для user-path; без автотестов риск регрессии перевешивает выигрыш в читаемости.

4. **Унификация Design-агентов** в `BaseParallelDesignAgent` — отказано по той же причине.

---

## Проверки качества после изменений

- ✅ Все Python-файлы (`py_compile`) — без ошибок
- ✅ TypeScript (`tsc --noEmit`) — без ошибок
- ✅ Runtime импорты резолвятся
- ✅ Grep на старые `_clean_json` / `_get_collection` — 0 совпадений
- ✅ Byte-identity SYSTEM-промптов — подтверждена
- ✅ 0 `logger.info` в коде, 3 `logger.error`/`exception` сохранены
